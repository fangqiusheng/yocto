{% extends 'knp_menu.html.twig' %}

{% block root %}
    {% import 'knp_menu.html.twig' as menu %}
    <ul{{ menu.attributes(item.attributes) }}>
        {{ block('children') }}
    </ul>
{% endblock %}
